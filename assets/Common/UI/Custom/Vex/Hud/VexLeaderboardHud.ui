$V = "../VexCommon.ui";

Group {
    Anchor: (Top: 50, Left: 0, Right: 0, Height: 220);
    LayoutMode: Middle;

    $V.@DecoratedContainer {
        Anchor: (Width: 680);

        #VexTitle {
            $V.@Title {
                @Text = %server.customUI.vexLeaderboard.title;
            }
        }

        #VexContent {
            LayoutMode: Top;
            Padding: (Full: 18);

            Label #VexLeaderboardBody {
                Text: "";
                Style: (...$V.@DefaultLabelStyle, Wrap: true, TextColor: #cdb6ff);
            }
        }
    }
}
